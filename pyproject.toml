[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pii_data_detection"
version = "1"
readme = "README.md"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = "src"